{
  "name": "notemeet-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix Frontend && npm install --prefix Backend",
    "dev": "concurrently --kill-others --names \"Frontend,Backend\" --prefix-colors \"blue,green\" \"npm run dev --prefix Frontend\" \"npm run dev --prefix Backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
